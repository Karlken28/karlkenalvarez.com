version: 1
frontend:
  phases:
    build:
      commands:
        # No build step needed, just echo for logs
        - echo "No build step required for static HTML"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
